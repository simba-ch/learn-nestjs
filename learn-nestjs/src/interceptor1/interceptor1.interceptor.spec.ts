import { Interceptor1Interceptor } from './interceptor1.interceptor';

describe('Interceptor1Interceptor', () => {
  it('should be defined', () => {
    expect(new Interceptor1Interceptor()).toBeDefined();
  });
});
